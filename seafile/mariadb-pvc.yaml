apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seafile-mariadb-data-pvc
  namespace: seafile
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
    volumeName: seafile-mariadb-data-pv
  